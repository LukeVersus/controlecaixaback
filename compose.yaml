services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=controlecaixa'
      - 'POSTGRES_PASSWORD=80lvcr58'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
